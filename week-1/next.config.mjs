/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['wallpapers.com','upload.wikimedia.org'] // <== Domain name
  }
}

export default nextConfig;
